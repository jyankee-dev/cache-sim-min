# cache-sim-min
JS/Node implementation of a cache simulation with fewest lines possible (my attempt). Uses "prompt-sync" npm library for user input.

Currnetly uses 67 lines. Could be fewer without prompts and hints. How many can you do?
